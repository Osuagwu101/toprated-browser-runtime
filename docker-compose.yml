name: toprated-browser-runtime

services:
  api:
    build:
      context: ./api
    environment:
      APP_NAME: TopRatedBrowserRuntime
      APP_ENV: ${APP_ENV:-local}
      APP_DEBUG: ${APP_DEBUG:-false}
      APP_KEY: base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
      LOG_CHANNEL: stderr
    ports:
      - "127.0.0.1:${API_PORT:-18080}:8080"
    restart: unless-stopped
    networks:
      - runtime-private

  browser-worker:
    build:
      context: ./worker
    environment:
      PORT: 8081
      CHROMIUM_PATH: ${CHROMIUM_PATH:-/usr/bin/chromium}
      LOG_LEVEL: ${WORKER_LOG_LEVEL:-info}
      VIEWER_TOKEN_TTL_SECONDS: ${VIEWER_TOKEN_TTL_SECONDS:-600}
      VIEWER_FRAME_INTERVAL_MS: ${VIEWER_FRAME_INTERVAL_MS:-250}
    ports:
      - "127.0.0.1:${WORKER_PORT:-18081}:8081"
    restart: unless-stopped
    networks:
      - runtime-private

networks:
  runtime-private:
    driver: bridge
